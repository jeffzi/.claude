---
description: Review Python code for best practices and Pythonic idioms
argument-hint: "[file or code] [optional context]"
---

Use the `pythonic` skill to review the provided Python code.

**MANDATORY:** Follow the pythonic skill's Verification Rules:
- Use `uv run` for ALL commands (ruff, pytest, pre-commit)
- Never run bare `ruff`, `pytest`, `mypy` - always prefix with `uv run`
